name: Lint

on: [pull_request]

jobs:
  run-linter:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: 16.8.0

    - run: yarn install
    - run: yarn run lint
